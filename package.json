{
  "name": "unit_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "keywords": [
    "MDS",
    "Project",
    "unit",
    "tests"
  ],
  "author": "MDS",
  "devDependencies": {
    "coveralls": "^3.1.0",
    "jest": "^24.8.0"
  },
  "dependencies": {
    "axios": "^0.19.0"
  }
}
